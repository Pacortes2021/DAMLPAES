# DAML 2026 · Grupo 5 — DEPLOY del dashboard en Streamlit Cloud (Python 3.12).
# Versiones FIJAS y probadas: evita que un redeploy resuelva versiones nuevas que rompan la app
# (como pasó con Python 3.14). Solo lo que importa src/app.py → inference.py → areas.py.
# Para re-correr el pipeline (scripts/) localmente:  pip install -r requirements-scripts.txt
streamlit==1.50.0
numpy==2.0.2
pandas==2.3.3
scikit-learn==1.6.1
joblib==1.5.3
plotly==6.8.0
